/**
 *  @useEffect 
 * ! 1. Update State
 * ! 2. Update DOM (muatated)
 * ! 3. re-Render  UI
 * ! 4. call Cleanup method when dependencies changed
 * ! 5. call userEffect callback
 * 
 * 
 *  @userLayoutEffect 
 * 
 * ? 1. Update State
 * ? 2. Update DOM (muatated)
 * ? 3. call cleanup method when dependencies changed(sync)
 * ? 4. call userLayoutEffect callback
 * ? 5. re-Render UI
 */